#ifndef SORT_H
#define SORT_H

#include <stddef.h>

/* Prototype for printing an array of integers */
void print_array(const int *array, size_t size);

/* Prototype for Bubble Sort */
void bubble_sort(int *array, size_t size);

#endif /* SORT_H */
